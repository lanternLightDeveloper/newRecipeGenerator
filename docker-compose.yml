services:
  db:
    image: postgres:17
    container_name: recipes-dev-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: recipes
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev
